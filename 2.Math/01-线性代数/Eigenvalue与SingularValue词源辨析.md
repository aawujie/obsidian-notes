---
tags: [线性代数，术语，词源，翻译]
created: 2026-04-08
---
# Eigenvalue 与 Singular Value —— 词源与含义辨析

## 核心对比

| 术语  | 英文             | 字面意思         | 中文翻译      |
| --- | -------------- | ------------ | --------- |
| 特征值 | Eigenvalue     | **自身的值**     | 特征值 / 本征值 |
| 奇异值 | Singular Value | **单一方向的拉伸值** | 奇异值       |

---

## Eigenvalue（特征值/本征值）

### 词源

**Eigen** 来自德语，原意：
- **自身的**、**固有的**、**独特的**
- 英文对应：*own*, *intrinsic*, *characteristic*

所以 `Eigenvalue` 的字面意思是：
> "**固有的值**"或"**自身的值**"

### 为什么叫 Eigenvalue？

因为特征值描述的是矩阵**固有的属性**：
- 对于方阵 $A$，若 $Av = \lambda v$
- $\lambda$ 是矩阵 $A$ **自身的值**——不依赖于坐标系，是矩阵的固有属性
- 特征向量 $v$ 是**自身方向不变**的向量

### 中文翻译

| 译名 | 使用者 | 理由 |
|------|--------|------|
| **特征值** | 大陆主流 | 强调矩阵的"特征性质" |
| **本征值** | 台湾、物理界 | "本"=固有，更贴近 eigen 原意 |

**其他语言**：
- 法语：Valeur propre（固有的值）
- 日语：固有値（こゆうち）
- 俄语：Собственное значение（自身的值）

---

## Singular Value（奇异值）

### 词源

**Singular** 来自拉丁语 *singularis*，原意：
- **单一的**、**独特的**、**特殊的**
- 英文对应：*single*, *unique*, *distinctive*

### 为什么叫 Singular Value？

**核心理解**：
> "Singular"在这里不是"奇怪"，而是"**单一的/纯粹的**"

**含义**：
1. **单一方向的拉伸**：每个奇异值对应一个**特定的主轴方向**
2. **纯粹的伸缩**：只改变长度，不改变方向（不旋转）
3. **独特的属性**：这些值是矩阵 $A^TA$ 的特征值开方，是矩阵的**独特指纹**

### 几何直观

想象矩阵是一个线性变换，把单位圆变成椭圆：
- **奇异值** = 椭圆的**主轴长度**
- 每个奇异值对应一个**纯粹的主轴方向**
- 这些方向是"单一的"——只拉伸，不混合

### 中文翻译问题

**"奇异值"这个翻译的问题**：
- ❌ 容易让人联想到"奇怪"、"神秘"
- ❌ 掩盖了"单一方向拉伸"的本质
- ✅ 其实它就是"**主轴拉伸值**"或"**单一方向伸缩因子**"

**更好的理解方式**：
> Singular Value = "**单一方向的纯粹拉伸幅度**"

---

## 两者对比

### 词源对比

| | Eigenvalue | Singular Value |
|--|------------|----------------|
| **词源** | 德语 eigen（自身的） | 拉丁语 singularis（单一的） |
| **核心含义** | 固有的、自身的 | 单一的、独特的 |
| **强调点** | 这是矩阵**固有的属性** | 这是**单一方向的拉伸** |

### 几何含义对比

| | Eigenvalue | Singular Value |
|--|------------|----------------|
| **问题** | 哪个方向**不变**？ | 哪个方向**拉伸最大**？ |
| **答案** | 特征向量方向 | 奇异向量方向（主轴） |
| **值** | 伸缩倍数 $\lambda$ | 伸缩倍数 $\sigma$ |
| **适用范围** | 方阵 | 任意矩阵 |

### 为什么 Singular 不是"奇怪"？

中文翻译"奇异值"容易误解，但英文原意很清楚：
- **Singular** = **Single**（单一的）
- 每个奇异值属于一个**单一的主轴方向**
- 这个方向的拉伸是**纯粹的**（只伸缩，不旋转）

> **一句话理解**：
> - Eigenvalue = "**自身的值**"（矩阵固有的属性）
> - Singular Value = "**单一方向的拉伸值**"（每个主轴的伸缩幅度）

---

## 记忆技巧

### Eigenvalue
> **Eigen = Own = 固有**
> 
> 特征值 = 矩阵"**自己的值**"——它固有的、不变的属性

### Singular Value
> **Singular = Single = 单一**
> 
> 奇异值 = "**单一方向的纯粹拉伸**"——每个主轴的伸缩幅度

---

## 总结

| 术语 | 英文原意 | 中文翻译 | 更好的理解 |
|------|----------|----------|------------|
| Eigenvalue | 自身的值 | 特征值/本征值 | ✅ 固有值 |
| Singular Value | 单一方向的拉伸值 | 奇异值 | ✅ 主轴拉伸值 |

**共同点**：
- 都是描述矩阵的**伸缩变换**
- 都是矩阵的**固有属性**（不依赖于坐标系）

**区别**：
- Eigenvalue 找"**方向不变的向量**"
- Singular Value 找"**拉伸最大的方向**"

---

## 延伸阅读

- [[特征值与奇异值对比]] — 两者的数学定义和应用对比
- 线性代数教材：特征值分解与 SVD 章节

---

*创建于 2026-04-08*
